// Overwritten by build-env.js on every Netlify deploy.
// Capability flags only — never put secrets in this file (it is served publicly).
export const APP_CONFIG = {
  hasAI: false,
  hasImages: false,
  hasWeather: false,
  builtAt: null
};
